{
  "name": "zly-web",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev":"pnpm --recursion dev --filter @zly/web-app",
    "build": "pnpm run -r build --filter @zly/chunks-file-upload",
    "start:server": "pnpm -r start --filter @zly/fileupload-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
